<%= form_for([@feature, @feature_link]) do |f| %>
  <% if @feature_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feature_link.errors.count, "error") %> prohibited this color from being saved:</h2>

      <ul>
      <% @feature_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link_url %><br>
    <%= f.text_field :link_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
